package io.vacivor.nexo.oauth2.store;

import io.vacivor.nexo.core.AccessTokenStore;
import io.vacivor.nexo.oauth2.token.OAuth2AccessToken;

public interface OAuth2AccessTokenStore extends AccessTokenStore<OAuth2AccessToken> {
}
